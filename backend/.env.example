# Copy to .env and fill in real values locally — never commit the real file.
# Used by: supabase/functions/place-accessibility (Owner 1)

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Apify token — only ever read from the Edge Function, never from the iOS app.
APIFY_TOKEN=
